<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<p>
  <strong>Picture:</strong>
  <%= image_tag @image.picture.url, size: "300x400" if @image.picture? %>
</p>


<%= link_to 'Edit', edit_image_path(@image), class: "btn btn-primary button" %> |
<%= link_to 'Back', images_path, class: "btn btn-secondary button" %>
